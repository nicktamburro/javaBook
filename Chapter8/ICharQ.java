package qpack;
//the character Queue interface
public interface ICharQ{
	//put a character in
	void put(char ch);
	//get a character out
	char get();
}
//that's it! Now each implementation will define what actually happens

